---
name: ts
uri: druby://localhost:30000
notify:
  - address: 224.0.0.1
    port: 30010
  - address: 224.0.0.1
    port: 30011
filters:
  - [ ~, ~ ]
  - [ ~, ~, ~]
  - [ ~, ~, ~, ~]
  - [ ~, ~, ~, ~, ~]
adapter:
  host: localhost
  port: 30001
  max_clients: 32
