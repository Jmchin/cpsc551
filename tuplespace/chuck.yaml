---
name: ts
uri: druby://localhost:40000
notify:
  - address: 224.0.0.1
    port: 40010
  - address: 224.0.0.1
    port: 40011
filters:
  - [ ~, ~ ]
  - [ ~, ~, ~]
  - [ ~, ~, ~, ~]
  - [ ~, ~, ~, ~, ~]
adapter:
  host: localhost
  port: 40001
  max_clients: 32
