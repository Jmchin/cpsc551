---
name: ts
uri: druby://localhost:20000
notify:
  - address: 224.0.0.1
    port: 20010
  - address: 224.0.0.1
    port: 20011
filters:
  - [ ~, ~ ]
  - [ ~, ~, ~]
  - [ ~, ~, ~, ~]
  - [ ~, ~, ~, ~, ~]
adapter:
  host: localhost
  port: 20001
  max_clients: 32
